"use strict";
/* const pessoa = {
    nome: 'Marina',
    idade: 28,
    profissao: 'desenvolvedora'
}

pessoa.idade = 23;

const andre: {nome: string, idade: number, profissao: string} = {
    nome: 'Andre',
    idade: 24,
    profissao: 'pintor'
}

const paula: {nome: string, idade: number, profissao: string} = {
    nome: 'Paula',
    idade: 23,
    profissao: 'Desenvolvedora'
}

enum Profissao {
    Professor,
    Atriz,
    Desenvolvedora,
    JogadoraDeFutebol
}
// enum serve para criar um grupo de constantes

interface Pessoa {
    nome: string,
    idade: number,
    profissao: Profissao
}

interface Estudante extends Pessoa  {
    materias: string[]
}

const vanessa: Pessoa = {
    nome: 'Vanessa',
    idade: 23,
    profissao: Profissao.Desenvolvedora
}

const maria: Pessoa = {
    nome: 'Maria',
    idade: 25,
    profissao: Profissao.Atriz
}

const joao: Estudante = {
    nome: 'João',
    idade: 18,
    materias: ['ADS', 'MySQL']
}

const monica: Estudante = {
    nome: 'Monica',
    idade: 34,
    materias: ['ADS', 'MySQL']
}

function listar (lista: string[]) {
    for (const item of lista) {
        console.log('- ', item);
    }
}

listar(monica.materias)

//extends: siginifica que aquela pessoa que vier da interface extendida
// receberá todos os mesmos requisitos de identificação (nome, idade, profissão)
//para verificar isso basta apertar o comando ctrlspace
//listas: listas no ts será identificado como o tipo da lista e colchetes
//ex: string[]
//?: quando colocado ? após o atributo de uma interface aquele atributo
//deixará de ser obrigatório responder e o programa encontrará 0 erros
*/ 
